# Content Model

## Project metadata

TODO: Finalize fields for `content/projects/<id>/metadata.json`.

Current placeholder fields:

- `id`
- `name`
- `description`
- `version`
- `status`
- `category`

## Optional content files

TODO: Define optional README / body / screenshots references.

## Registry output

TODO: Document shape of `generated/registry.json`.

## Validation rules

TODO: Required fields, enums for `status` / `category`, asset existence checks.

## Ownership

TODO: Who may add or edit project content.
