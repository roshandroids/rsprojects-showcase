# assets/icons

**Why:** App and UI icons used across the portal.

**Owner:** Design / frontend.

**When:** Add icons as features need them; register in `pubspec.yaml`.

## Rules

- Keep naming consistent (`ic_<name>.svg` / `.png`).
- Prefer a single icon set style.
