/// Pure utility helpers (formatting, parsing, path helpers).
///
/// **Why:** Stateless helpers shared across layers without UI or DI.
/// **Owner:** Core platform.
/// **When:** Add utilities only when duplication appears.
library;

/// Placeholder for core utilities.
///
/// TODO(core): Add formatters, validators, and content path helpers.
abstract final class CoreUtilities {
  CoreUtilities._();
}
