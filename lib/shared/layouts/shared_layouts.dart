/// Shared page layouts (shell, responsive scaffolds).
///
/// **Why:** Consistent page chrome across home, projects, settings, etc.
/// **Owner:** Shared platform.
/// **When:** Implement with first multi-route navigation shell.
library;

import 'package:flutter/widgets.dart';

/// Placeholder for shared layouts.
///
/// TODO(shared): Add AppShellLayout, ContentWidthConstraint, etc.
class SharedLayoutsPlaceholder extends StatelessWidget {
  const SharedLayoutsPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
