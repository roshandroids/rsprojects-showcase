# Golden tests

Visual regression baselines (Home, Projects, nav, footer, cards, light/dark, responsive).

Add `*_test.dart` + golden PNGs when UI surfaces stabilize.

See `docs/quality/TESTING_STRATEGY.md`.
