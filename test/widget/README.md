# Widget tests

Reusable widgets, layouts, navigation, responsive behavior, theme switching.

Existing shell tests currently live under `test/*_test.dart` and may migrate here over time.

See `docs/quality/TESTING_STRATEGY.md`.
